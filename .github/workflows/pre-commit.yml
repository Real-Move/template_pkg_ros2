name: Pre-Commit Checks

on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - main
      - devel

jobs:
  pre-commit-check:
    uses: Real-Move/realmove-actions/.github/workflows/reusable-pre-commit.yml@main
